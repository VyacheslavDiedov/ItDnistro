@model IEnumerable<DataBase.TourType>

@foreach (var category in Model)
{
    <li>
        @Html.ActionLink(@category.TourTypeName, "GetTourInfo", "Dashboard",
            new { idTour = @category.Id })
    </li>
}
